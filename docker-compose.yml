version: "3"

services:
  api-solid-pg:
    image: bitnami/postgresql
    ports:
      - 5432:5432
    environment:
      - POSTGRESQL_USERNAME=dockerpg
      - POSTGRESQL_PASSWORD=dockerpg
      - POSTGRESQL_DATABASE=apisolid
